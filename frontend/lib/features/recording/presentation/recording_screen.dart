import 'package:flutter/material.dart';

// TODO: Recording — POST /recordings/start + /upload/init per the sequence diagram.
class RecordingScreen extends StatelessWidget {
  const RecordingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('RecordingScreen — placeholder')));
  }
}
